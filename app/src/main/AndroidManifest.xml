<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logopetcare"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logopetcare"
        android:supportsRtl="true"
        android:theme="@style/Theme.PetCare">
        <activity
            android:name=".dialog_edit_person"
            android:exported="true" />
        <activity
            android:name=".PersonDetailsActivity"
            android:exported="true"
            android:theme="@style/Theme.PetCare" />
        <activity
            android:name=".AgendarCitaActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="true" />
        <activity
            android:name=".CreatePetProfileActivity"
            android:exported="true" />
        <activity
            android:name=".NewAccountActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>